// LECTURE: Values and Variables
let country = 'Sweden';
let continent = 'Europe';
let population = 9;
console.log('I live in ' + country + '. ' + country + ' is part of ' + continent + '. ' + population + ' million people lives in my country.');

// LECTURE: Data Types
let isIsland = false;
let consoleLogText = country + ' is';
if (isIsland == false) consoleLogText = consoleLogText + ' not';
consoleLogText = consoleLogText + ' part of an island.';
console.log(consoleLogText);

// LECTURE: let, const and var 
let language = "Swedish";
console.log('The prefered language in my country is ')